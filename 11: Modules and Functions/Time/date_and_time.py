# import time## print("the epoch on this system starts at: " + time.strftime("%c", time.gmtime(0)))# print("The current timezone is {} with an offset of {}.".format(time.tzname[0], time.timezone))## if time.daylight != 0:#     print("\tDaylight saving is not in effect for this location.")#     print("\tThe DST timezone is " + time.tzname[1])## print("Local time is " + time.strftime("%Y-%m-%d %H:%M:%S", time.localtime()))# print("UTC time is " + time.strftime("%Y-%m-%d %H:%M:%S", time.gmtime()))import datetimeprint()# milliseconds will be slightly diff because of when intellij processed codeprint(datetime.datetime.today())print(datetime.datetime.now())print(datetime.datetime.utcnow())